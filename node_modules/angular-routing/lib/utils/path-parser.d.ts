import { Route } from '../route';
import { Params } from '../route-params.service';
export interface RouteMatch {
    path: string;
    params: Params;
}
export declare function getPathSegments(route: Route): string[];
export declare const parsePath: (route: Route) => RegExp;
export declare const matchRoute: (url: string, route: Route) => RouteMatch;
